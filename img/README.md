# Exercise photos

Drop photos here named `<slug>.jpg` — see `../PHOTOS.md` for the full filename list.

Any exercise without a photo falls back to its drawn diagram automatically, so
you can add them a few at a time and nothing ever breaks.
