/* ============================================================
   YOUR FIREBASE KEYS — edit this file, never index.html.
   ------------------------------------------------------------
   App updates overwrite index.html only, so your sign-in keeps
   working across every future update.

   Replace the values below with the firebaseConfig object from:
   Firebase console → ⚙ → General → Your apps → Web app.

   These values are public by design. They identify the project;
   they do not grant access. Access is controlled by
   firestore.rules, which limits every user to their own record.
   ============================================================ */
window.FIREBASE_CONFIG = {
  apiKey: "PASTE_YOUR_API_KEY",
  authDomain: "PASTE_YOUR_PROJECT.firebaseapp.com",
  projectId: "PASTE_YOUR_PROJECT_ID",
  storageBucket: "PASTE_YOUR_PROJECT.appspot.com",
  messagingSenderId: "PASTE_YOUR_SENDER_ID",
  appId: "PASTE_YOUR_APP_ID"
};
